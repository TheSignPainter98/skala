#!/bin/sh
set -eu
YUE=/home/workshop/.local/bin/yue
if [ -x "$YUE" ] && "$YUE" --version >/dev/null 2>&1; then
  workshopctl set-health okay
else
  workshopctl set-health error "yue is not installed or failed to run"
fi
