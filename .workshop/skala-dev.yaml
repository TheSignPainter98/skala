name: skala-dev
base: ubuntu@24.04
connections:
  - plug: omp:omp-home
    slot: system:mount
sdks:
  - name: direnvrc
  - name: rust
  - name: omp
  - name: project-yuescript
actions:
  omp: direnv exec /project omp "$@"
  yue: direnv exec /project yue "$@"
  direnv: direnv "$@"
