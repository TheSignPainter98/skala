export macro T = (ty, expr) ->
  if 'release' == os.getenv 'PROFILE'
    expr
  else
    "T #{ty}, #{expr}"

export macro F = (ty, expr) ->
  if 'release' == os.getenv 'PROFILE'
    expr
  else
    "F #{ty}, #{expr}"
